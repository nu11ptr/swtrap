pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://repo.spring.io/release' }
    }
}

rootProject.name = 'swtrap'
include 'shared'
include 'swt'
include 'rwt'
